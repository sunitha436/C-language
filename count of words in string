#include<stdio.h>
#include<string.>
#include<ctype.h>
int main(){
char str[100];
int i,words=0;
int inwords=0;
fgets(str,sizeof(str),stdin);
str[strcspn(str,"\n")}='\0';
  for(i=0;str[i]!='\0';i++){
  if(!isspace(str[i])){
  if(inwords==0){
  words++;
  inwords=1;}
  }
  else{
  inwords=0;
  }
  }
  printf("number of words:%d\n",words);
  return 0;
  }
